def attack(core, target):
    from wifite3.utils import log
    log(f"[yellow]Bruteforce attack sur {target}...[/yellow]")
    # TODO: Brute-force (wordlist+charset, rainbow tables, multi-process, etc.)
    return False