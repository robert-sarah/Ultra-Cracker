def scan(core):
    from wifite3.utils import log
    # TODO: Intégrer scan de tous les APs (airodump-ng, iw, etc.)
    log("[yellow]Scan réseaux (squelette). À compléter avec airodump-ng et parsing CSV.[/yellow]")
    return []  # Mettre ici la liste de cibles au format dict